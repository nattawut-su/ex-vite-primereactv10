# วิธีใช้ environment -> cp .env.integration.example .env.integration

# เปิดใช้ backend จริง ไม่ใช้ mock
VITE_USE_MOCK=false
# URL ของ backend integration server (เช่น UAT / QA server)
VITE_API_BASE_URL=https://integration-api.company.example